//               -5       -4       -3         -2          -1
//                0        1        2          3           4
const nomes = ['Lucas', 'João', 'Matheus', 'Guilherme', 'Felipe'];

// O método splice() altera o conteúdo de uma lista, adicionando novos elementos enquanto remove elementos antigos.

// Diferente do Pop, ele retorna como Array

// nomes.splice(índice atual, delete, elem1, elem2, elem3...);

// Pop
//const removidos = nomes.splice(-1, 1);

// Shift
//const removidos = nomes.splice(0, 1);

// Push
//const adicionando = nomes.splice(nomes.length, 0, 'Cila');

// Unshift
//const adicionando = nomes.splice(0, 0, 'Cila');

console.log(nomes, adicionando);
